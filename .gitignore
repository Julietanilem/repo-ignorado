**/ContenidoSecreto/
# El archivo config ya que posee información confidencial 
# /dynamics/php/config.php
# (Pero necesita que exista una plantilla ya que a veces se le va la onda)

# Todos los archivos con extensión pdf dentro de los assets, pero no los que se encuentran en la carpeta docs
/assets/**/*.pdf

!**/assets/docs/*.pdf

# Todos los archivos de extensión txt pues no poseen información importante
**/*.txt
# Todas las imágenes a excepción de las esenciales para el sistema (Sabrás cuales no deben eliminarse observando el nombre de los archivos)
/assets/media/images/*.*
!/assets/images/*-system*
# Todos los archivos marcados con la bandera -prueba o -local (Por ejemplo: modales-local.js o sesiones-prueba.php) 
# a menos que se marquen como -experimento en cualquier parte del nombre
**/*-prueba*.*
**/*-local*.*
!**/*-experimento*.*